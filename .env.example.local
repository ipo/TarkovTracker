# Empty pair: local-storage mode. Set both or neither; one alone fails boot.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Static exporter JSON. Leave the base URL relative to serve files from public/quest-data.
NUXT_PUBLIC_STATIC_QUEST_DATA=true
NUXT_PUBLIC_STATIC_QUEST_DATA_BASE_URL=/quest-data
